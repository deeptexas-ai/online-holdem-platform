# 牌局流程与状态管理

多人德州扑克服务端需要将每个请求限制在明确状态内。`gameroot.cpp`、`gamestation.cpp` 与 `process/` 是梳理状态流转的重要入口。

## 建议验证的流程

1. 玩家进入房间并取得牌桌状态。
2. 玩家满足条件后坐下或准备。
3. 服务端初始化牌局并确定行动顺序。
4. 各下注轮次按规则推进。
5. 结算并保存可审计结果。
6. 玩家继续、离桌或断线重连。

需要测试非法行动、重复消息、超时、断线、并发请求、人数边界和服务重启。所有关键状态与结算应由服务端验证，不能信任客户端计算结果。

